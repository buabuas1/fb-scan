import { KendoDatePickerDirective } from './kendo-date-picker.directive';

describe('KendoDatePickerDirective', () => {
  it('should create an instance', () => {
    const directive = new KendoDatePickerDirective();
    expect(directive).toBeTruthy();
  });
});
