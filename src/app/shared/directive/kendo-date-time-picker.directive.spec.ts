import { KendoDateTimePickerDirective } from './kendo-date-time-picker.directive';

describe('KendoDateTimePickerDirective', () => {
  it('should create an instance', () => {
    const directive = new KendoDateTimePickerDirective();
    expect(directive).toBeTruthy();
  });
});
